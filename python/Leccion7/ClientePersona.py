from Persona import * # Se importa la clase Persona y la clase Empleado
persona1 = Persona('Sofia', 8) # Creamos un objeto de la clase Persona
print(persona1) # Imprime el espacio de memoria hexadecimal, para ver los datos primero hay que definir el método to string en la clase Persona

empleado3 = Empleado('Ro', 31, 80000)
print(empleado3)
